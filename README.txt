This repository is a project in Python that permited to have a
connection with files .txt 
the project read a file Entrada.txt, which  contains a list, 
Python do a query SqlLite 
depending the case of list

creates other file archivosalida.txt with a string
Said program always is searching to changes in file Endrada.txt 
through of process that execute in background (thread)
